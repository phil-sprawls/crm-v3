# =============================================================================
# IT Platform CRM - Environment Configuration Template
# =============================================================================
# 
# Copy this file to .env and fill in your actual values:
#   cp .env.template .env
#
# IMPORTANT: Never commit .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (REQUIRED)
# -----------------------------------------------------------------------------

# Local Development (PostgreSQL)
DATABASE_URL=postgresql://username:password@localhost:5432/dev_psprawls

# Azure PostgreSQL (for production/staging)
# DATABASE_URL=postgresql://username:password@server.postgres.database.azure.com:5432/dev_psprawls?sslmode=require

# Use in-memory sample data instead of database (for testing only)
USE_SAMPLE_DATA=false

# -----------------------------------------------------------------------------
# Azure AD JWT Authentication (OPTIONAL)
# -----------------------------------------------------------------------------
# Required only if you want to secure the API with Azure AD authentication
# See AZURE_DEV_SETUP.md for setup instructions

# Azure AD App Registration Client ID
# APP_CLIENT_ID=your-azure-ad-app-client-id-here

# Azure Tenant ID
# TENANT_ID=your-azure-tenant-id-here

# OpenAPI UI Client ID (for interactive API docs)
# OPENAPI_CLIENT_ID=your-openapi-client-id-here

# -----------------------------------------------------------------------------
# Email Notifications (OPTIONAL)
# -----------------------------------------------------------------------------
# Required only if you want email notifications for intake requests
# Uses Azure Communication Services

# Admin email to receive intake request notifications
# ADMIN_EMAIL=admin@yourcompany.com

# Azure Communication Services connection string
# AZURE_COMMUNICATION_CONNECTION_STRING=endpoint=https://...;accesskey=...

# -----------------------------------------------------------------------------
# Azure Deployment (REQUIRED for Azure App Service)
# -----------------------------------------------------------------------------

# Enable build during deployment on Azure
# SCM_DO_BUILD_DURING_DEPLOYMENT=true

# Environment identifier
# ENVIRONMENT=development
# ENVIRONMENT=production

# -----------------------------------------------------------------------------
# Session Configuration (OPTIONAL)
# -----------------------------------------------------------------------------

# Session secret for secure cookies (auto-generated if not provided)
# SESSION_SECRET=your-random-secret-key-here

# -----------------------------------------------------------------------------
# Usage Instructions
# -----------------------------------------------------------------------------
#
# 1. Local Development:
#    - Copy this file: cp .env.template .env
#    - Set DATABASE_URL to your local PostgreSQL
#    - Set USE_SAMPLE_DATA=false (or true for demo mode)
#    - JWT and email settings are optional
#
# 2. Azure Deployment:
#    - Set environment variables in Azure App Service settings (not .env file)
#    - Required: DATABASE_URL, USE_SAMPLE_DATA, SCM_DO_BUILD_DURING_DEPLOYMENT
#    - Optional: JWT settings, email settings
#
# 3. Database Migration:
#    - Run: python migrate_db.py (creates tables)
#    - Run: python seed_azure_db.py (adds sample data)
#
# For complete setup instructions, see:
# - LOCAL_SETUP.md (local development)
# - AZURE_DEV_SETUP.md (Azure deployment)
# -----------------------------------------------------------------------------
